TODO:

* [x] - check for the errors in frontend (browser f12)
* [x] - create an email/sms sender
* [x] - add the shipping data to data base
* [x] - privating git
    [x] - push to main beta version
* [ ] - AMAZON - AWS!
* [ ] - organize all the folders (add routes folder ... etc)




      // SCSS files
      {
        test: /\.scss$/,
        loader: ExtractTextPlugin.extract({
          fallback: 'style-loader',
          use: [
            {
              loader: 'css-loader',
              options: {
                'sourceMap': true,
                'importLoaders': 1
              }
            },
            {
              loader: 'postcss-loader',
              options: {
                plugins: () => [
                  autoprefixer
                ]
              }
            },
 
          ]
        })
      }